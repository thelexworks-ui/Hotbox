'use client';

// Canonical location for NeuralGlobe — re-exports from the preview implementation.
// The preview route keeps its own copy; this path is the one used by DashboardContent.
export { default } from '@/app/preview/dashboard-v2/NeuralGlobe';
