'use client';

export default function WorkspaceSettingsPage() {
  return (
    <div>
      <div className="mb-8">
        <h1 className="text-[22px] font-semibold text-[var(--hotbox-text)]">Workspace</h1>
        <p className="text-[13px] text-[var(--hotbox-text-muted)] mt-1">Manage your organization, members, and workspace settings</p>
      </div>
      <div
        className="flex items-center justify-center h-40 rounded-[12px] text-[13px] text-[var(--hotbox-text-dim)]"
        style={{ border: '1px dashed var(--hotbox-border)' }}
      >
        Coming soon
      </div>
    </div>
  );
}
