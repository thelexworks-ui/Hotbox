'use client';

export default function AgentsSettingsPage() {
  return (
    <div>
      <div className="mb-8">
        <h1 className="text-[22px] font-semibold text-[var(--hotbox-text)]">Agents</h1>
        <p className="text-[13px] text-[var(--hotbox-text-muted)] mt-1">Control agent behavior and per-agent permissions</p>
      </div>
      <div
        className="flex items-center justify-center h-40 rounded-[12px] text-[13px] text-[var(--hotbox-text-dim)]"
        style={{ border: '1px dashed var(--hotbox-border)' }}
      >
        Coming soon
      </div>
    </div>
  );
}
