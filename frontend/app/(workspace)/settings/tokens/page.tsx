'use client';

export default function TokensSettingsPage() {
  return (
    <div>
      <div className="mb-8">
        <h1 className="text-[22px] font-semibold text-[var(--hotbox-text)]">API Tokens</h1>
        <p className="text-[13px] text-[var(--hotbox-text-muted)] mt-1">Create and manage API tokens for integrations</p>
      </div>
      <div
        className="flex items-center justify-center h-40 rounded-[12px] text-[13px] text-[var(--hotbox-text-dim)]"
        style={{ border: '1px dashed var(--hotbox-border)' }}
      >
        Coming soon
      </div>
    </div>
  );
}
